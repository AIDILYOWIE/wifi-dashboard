export const TableHead = ({width = 'w-fill'}) => {
    return (
        <div className={`${width}  text-left bg-background`}>
            <p className="text-text text-[16px] font-semibold"></p>
        </div>
    )
}